# Security

Report suspected vulnerabilities privately to the repository owner through GitHub's private reporting channel when available.

Never attach or commit local inventory snapshots, SQLite databases, usernames, absolute user paths, probe output, credentials, or other machine-specific data. V1 must remain read-only and must not introduce arbitrary command execution or unattended software changes.

# Contributing

Branch from `develop` and return normal changes through a pull request to `develop`. Promote releases from `develop` to `main` through a separate pull request.

Run the locked restore, build, and behavioral checks before opening a pull request. Preserve the read-only v1 boundary, keep platform failures explicit, and update architecture, test, change, and runbook documentation with substantive changes.
